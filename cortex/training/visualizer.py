class Visualizer(object):

    '''
    if (save_images is not None and
        out_path is not None and
        (show_every is None or ((e + 1) % show_every == 0))):
        print('Saving images...')
        save_images()
    '''

    pass